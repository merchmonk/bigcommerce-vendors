| Issue | Bug Fix | Fix Status (pending, confirmed, or failed) | Fix Datetime |
|---|---|---|---|
| Lambda package size exceeded restrictions due to multiple prisma clients | Removed the local macOS Prisma client from the deploy artifact. | Confirmed | 03/21/2026 03:22:01 PM EST |
| BigCommerce category creation failed on vendor taxonomy strings that exceeded category name limits and blocked the full sync. | Prefer hierarchical vendor categories, drop invalid overlong category segments before create, and soft-skip BigCommerce `422` invalid-name category errors instead of failing the whole product sync. | Confirmed | 03/22/2026 01:40:00 PM EST |
| Catalog sync jobs stayed `ENQUEUED` after terminal BigCommerce validation failures, and cancel requests from the UI failed. | Treat BigCommerce `4xx` validation failures as non-retryable in the integration worker and update the vendor sync UI to cancel via `POST` with `action: cancel`. | Confirmed | 03/22/2026 03:35:00 PM EST |
| BigCommerce product creation failed because PromoStandards weight data was not being mapped into the outbound product payload. | Normalize product and variant weight from PromoStandards `Dimension.weight` and include `weight` in the BigCommerce product payload. | Confirmed | 03/22/2026 04:05:00 PM EST |
| BigCommerce price list upsert failed because price list records were missing `currency`. | Add `currency` to every projected price list record before upsert. | Confirmed | 03/22/2026 04:20:00 PM EST |
| PromoStandards endpoint discovery assumed normalized endpoint URL patterns, which broke vendors that expose nonstandard endpoint paths like Spector `productpriceandconfiguration` or vendor-specific service bindings. | Harvest resolved endpoint URLs from `CompanyData` service details, store them on discovered capability rows, persist them into per-mapping runtime config, and allow manual endpoint path overrides from the vendor UI. | Confirmed | 03/22/2026 08:10:00 PM EST |
| PromoStandards manual endpoint overrides had to be entered once per operation row, and targeted URI tests failed on valid endpoints when the SOAP service returned expected validation faults instead of HTTP `200`. | Group the vendor UI by endpoint and version so one manual path applies to every operation on that endpoint version, and run targeted endpoint tests through the PromoStandards WSDL-aware probe logic instead of the generic SOAP `200`-only connection test. | Confirmed | 03/22/2026 08:55:00 PM EST |
| Explicit PromoStandards endpoint override paths were still being normalized a second time by the SOAP client, causing valid paths like `/orderstatus/2.0.0` to become invalid doubled URLs such as `/orderstatus/2.0.0/orderstatusservice/2.0.0`. | Treat any already versioned endpoint path as final in the SOAP URL resolver so manual overrides and resolved service-detail URLs are not appended with the endpoint name a second time. | Failed | 03/22/2026 09:18:00 PM EST |
| ProductCompliance calls failed because the canonical mapping used the legacy operation name `getComplianceData` even though vendors expose the operation as `getCompliance`. | Correct the PromoStandards seed to use `getCompliance` and add a legacy SOAP operation alias so existing mapped vendors still call `getCompliance` at runtime. | Confirmed | 03/22/2026 09:28:00 PM EST |
| PricingAndConfiguration calls to Spector failed with `Currency ID not found` because the pricing request payload omitted the required `currency` field. | Add a default pricing `currency` request field for PromoStandards `PricingAndConfiguration` calls, with runtime config still able to override it when needed. | Confirmed | 03/22/2026 10:02:00 PM EST |
| PromoStandards PricingAndConfiguration auxiliary operations were still missing vendor-required derived fields, causing Spector pricing calls like `getDecorationColors` and `getConfigurationAndPricing` to fail even after adding `currency`. | Order pricing operations so prerequisite lookups run first, derive `locationId` and `fobId` from earlier pricing responses, and default `getConfigurationAndPricing` to `priceType=List` and `configurationType=Blank` unless runtime config overrides them. | Confirmed | 03/22/2026 10:15:00 PM EST |
| BigCommerce product updates failed with duplicate custom field errors like `vendor_endpoint=ProductData already exists` because product `PUT` payloads resent custom fields that BigCommerce treats as additive. | Keep product custom fields on create, but omit them from BigCommerce product update payloads so existing custom fields are preserved instead of duplicated. | Confirmed | 03/22/2026 10:33:00 PM EST |
| BigCommerce price-list pricing could silently fail to attach on first sync because newly created variant IDs were discarded and the sync relied on an immediate variant re-list that may not include fresh variants yet. | Preserve variant IDs returned by BigCommerce create/update responses and use them directly when building price-list records, merging in any later re-list results instead of depending on the re-list alone. | Confirmed | 03/22/2026 10:52:00 PM EST |
| Parent products with option variants could still show no base pricing in BigCommerce because the sync only wrote price-list records for option variants and never attached the fallback base price to the product `base_variant_id`. | Always map the BigCommerce `base_variant_id`, keep fallback `price` and `cost_price` on the parent product payload, and add a base-variant price-list record for variant products using the projected product fallback price. | Confirmed | 03/22/2026 11:04:00 PM EST |
| BigCommerce modifier updates failed with `An option or modifier must have not more than one default` because modifier `PUT` payloads resent `is_default` flags while BigCommerce still retained an existing default option value. | Keep `is_default` on modifier creates, but omit it from modifier update payloads so updates don’t introduce a second default. | Confirmed | 03/22/2026 11:14:00 PM EST |
| BigCommerce modifier updates could still fail with duplicate option-label errors like `The option label: "6" value is already used on this option` because modifier `PUT` does not reliably replace existing option values. | When a managed modifier update returns a duplicate/validation-style `422`, delete that modifier and recreate it instead of failing the full sync. | Confirmed | 03/22/2026 11:24:00 PM EST |
| Vendor row product stats could stay at `0 synced / 0 active` even after a BigCommerce product already existed in the storefront, because later sync failures prevented `vendor_product_map` from being written. | Propagate partial BigCommerce upsert details when the product record already exists, and persist `vendor_product_map` plus `records_written` from the runner even if a later post-upsert step fails. | Confirmed | 03/22/2026 11:39:00 PM EST |
| BigCommerce variant creation could fail with `422` after product options were created because the sync still posted variants using `option_display_name` and `label` instead of the BigCommerce option ID and option-value ID pairs required by the standalone variants endpoint. | Re-read product options after ensuring they exist, build an option/value ID lookup, and send variant `option_values` using `{ option_id, id }` when those IDs are available. | Confirmed | 03/22/2026 11:58:00 PM EST |
| BigCommerce variant creation could still fail with `409 Variant with the same option values set exists` when vendor parts normalized to duplicate option combinations or when a matching variant already existed by combination during a retry/update path. | Make normalized variant option combinations unique by appending a `Part` option to colliding variants, and when variant create hits the duplicate-combination `409`, refresh existing variants and update the matching variant instead of failing the sync. | Confirmed | 03/23/2026 12:12:00 AM EST |
| Catalog syncs could fail too late on missing-data products, either halting a run after several successful writes or wasting effort when early products were all blocked by enrichment failures. | Let blocked products be reported and skipped once the run proves healthy, but add two safety rails in the runner: fail fast if the first 2 evaluated products both fail enrichment, and fail the job once at least 100 products have been attempted and 50% or more are blocked. | Confirmed | 03/23/2026 12:29:00 AM EST |
| BigCommerce product bulk pricing rule creation failed with `422 Bulk pricing rule property quantityMax should be set` when vendor tiers had an open-ended upper bound. | Always send `quantity_max` for catalog bulk pricing rules, defaulting open-ended tiers to `0` instead of omitting the field. | Failed | 03/23/2026 12:41:00 AM EST |
| BigCommerce product create still failed with `422 Bulk pricing rule property quantityMax should be set` because bulk pricing rules were also being sent inline on the base product payload before the dedicated bulk-pricing sync ran. | Stop sending inline `bulk_pricing_rules` on the base product create/update payload and let the dedicated post-upsert bulk-pricing sync own all catalog bulk pricing writes. | Confirmed | 03/23/2026 12:49:00 AM EST |
| BigCommerce bulk pricing rule creation could still fail with `409 ... quantity range ... must not overlap` because multiple vendor bulk tiers with only `quantityMin` were being sent as overlapping open-ended ranges. | Normalize catalog bulk pricing ranges before write so each tier ends just before the next tier starts, and only the final tier remains open-ended. | Confirmed | 03/23/2026 12:56:00 AM EST |
| BigCommerce price list record upserts failed with `400 ... type missing / amount missing` because nested `bulk_pricing_tiers` were still being sent in the wrong shape. | Change price list bulk tiers to use BigCommerce’s expected `{ quantity_min, quantity_max?, type: 'price', amount }` payload shape before writing price list records. | Confirmed | 03/23/2026 01:03:00 AM EST |
| Product inventory was landing in BigCommerce as `0` because inventory enrichment only stored a single product-level total and never mapped part-level inventory quantities back onto variants before variant sync. | Parse inventory responses into both a total and a per-part map, then apply matching quantities onto normalized variants so variant inventory writes use real vendor stock instead of falling back to `0`. | Confirmed | 03/23/2026 01:17:00 AM EST |
| Variant inventory could still be lost during product merges because duplicate variants were de-duped by SKU using a first-write-wins filter that discarded later non-empty fields. | Merge duplicate variants field-by-field during product consolidation in the normalizer and runner so later inventory values are preserved instead of dropped. | Confirmed | 03/23/2026 01:24:00 AM EST |
| Updated products could still send `inventory_level: 0` to BigCommerce because inventory enrichment defaulted `partId` to the product SKU or product ID, so Spector inventory calls were made with invalid part IDs like `BB196` instead of the real part IDs such as `00BB1960BLK`. | Stop sending the default fallback `partId` on `Inventory` endpoint calls and let the vendor return product-level or part-array inventory data, which is then matched back onto variants from the response payload. | Confirmed | 03/23/2026 02:42:00 AM EST |
| Existing products needed a temporary low-risk sync mode because repeated full updates were still causing instability, while the immediate business need was to keep storefront inventory current. | Add a temporary existing-product shortcut that skips the general BigCommerce update path and only updates inventory for already-synced products; new products still go through the normal create flow. | Confirmed | 03/23/2026 02:51:00 AM EST |
| Flat vendor category lists could still assign noisy non-taxonomy tags like `MADE IN CHINA` and `Branding solutions` to BigCommerce products because the fallback category normalizer only de-duped values when no hierarchical category path existed. | Keep the existing hierarchy-first behavior, but filter flat category values that start with `made in` and the tag `branding solutions` before assigning fallback BigCommerce categories. | Confirmed | 03/23/2026 01:36:00 AM EST |
| Catalog syncs could stop creating new products and then appear to loop over the same SKUs because `ProductData` discovery returned multiple `productId + partId` references for the same product, and the runner upserted each resolved full product again on later part references. | Track processed vendor product IDs within a sync run and skip duplicate product assemblies/upserts when later `ProductData` references resolve to a vendor product that was already processed earlier in the same run. | Failed | 03/23/2026 01:49:00 AM EST |
| Catalog syncs could still stall for a long time with no new products added because the duplicate-reference guard only skipped repeated writes after fetch, while the runner still called `getProduct` for every duplicate `productId + partId` reference. | Skip duplicate `ProductData` references before the vendor fetch by only calling `getProduct` once per `productId` within a sync run, with the later duplicate-product guard left in place as a safety backstop. | Confirmed | 03/23/2026 02:16:00 AM EST |
| Catalog syncs could still hang after a product image upload because the BigCommerce media sync still called the product video API even when no videos were desired, and the shared BigCommerce request helper had no timeout to fail a stalled request. | Keep the catalog media sync image-only for now by skipping BigCommerce video calls entirely, and add a shared timeout to `requestJson()` so a stalled BigCommerce request aborts instead of freezing the whole sync run. | Confirmed | 03/23/2026 02:28:00 AM EST |
| Cancelled or crashed catalog syncs could leave older `etl_sync_runs` rows stuck in `RUNNING`, so the sync panel showed multiple active-looking runs and made cancellation state confusing even when the latest integration job was already cancelled. | Reconcile stale catalog sync runs by marking older `PENDING/RUNNING` rows as failed once a newer active run exists, or when no active catalog sync job exists for the vendor, and run that cleanup before new syncs start and before the sync panel loads runs. | Confirmed | 03/23/2026 02:03:00 AM EST |
| The vendor sync panel repeatedly refetched sync and mapping APIs while idle, creating a near-continuous request loop in the UI. | Remove SWR auto-refresh and focus/reconnect revalidation from the vendor sync panel so data only reloads on page refresh or after explicit run/cancel actions. | Confirmed | 03/22/2026 08:32:00 PM EST |
| Vendor row action dropdowns on the vendors table rendered inside the table overflow container, so opening the ellipsis menu could create a small scroll box and clip actions like Sync now or Edit out of view. | Render the shared row action menu through a portal on `document.body` and anchor it with fixed viewport positioning so the dropdown overlays the table instead of being clipped inside it. | Confirmed | 03/22/2026 04:51:53 PM EDT |
| Inventory-only syncs could keep product and variant inventory at `0` because existing-product inventory updates matched variants by BigCommerce SKU only and defaulted missing inventory writes to zero. | Match existing variants by normalized SKU or `Part` option label, and omit inventory writes when no vendor inventory value is present instead of forcing `0`. | Confirmed | 03/23/2026 10:35:00 AM EDT |
| Existing-product inventory-only syncs could still leave inventory nonfunctional because the update branch wrote counts but never repaired the BigCommerce product’s `inventory_tracking` mode. | When running the inventory-only update path, explicitly set `inventory_tracking` to `product` for simple products and `variant` for variant products before applying inventory values. | Confirmed | 03/23/2026 10:49:00 AM EDT |
| BigCommerce product image descriptions were missing variant, location, and decoration associations, and media parsing could pollute location data with unrelated numeric/address fields. | Tighten PromoStandards media identifier extraction, preserve location and decoration names on normalized media assets, and include both IDs and resolved names in the `mm_media` marker stored on uploaded BigCommerce images. | Confirmed | 03/23/2026 10:35:00 AM EDT |
| PromoStandards `availableLocations` data from Spector exposed a supplier address rather than decorator placement options, which made downstream decoration-location data misleading. | Stop projecting `availableLocations` into the designer contract and rely on `Configuration.LocationArray.Location` for decoration placement data instead. | Confirmed | 03/23/2026 10:35:00 AM EDT |
| Full catalog syncs could time out at the 15-minute Lambda limit after only partially processing a large vendor catalog, requiring manual reruns to continue the import. | Split ProductData-driven catalog syncs into resumable batches using a continuation offset, preserve the original discovery checkpoint across batches, and automatically enqueue the next batch only after the current batch completes successfully. | Confirmed | 03/23/2026 11:40:00 AM EDT |
| BigCommerce inventory quantities could stay at `0` because the sync was still writing quantities through Catalog product and variant `PUT` calls instead of the Inventory API path BigCommerce now expects. | Stop sending quantity through Catalog product and variant payloads, build inventory sync targets during product upsert, and flush batch inventory at the end of each sync run through `PUT /v3/inventory/adjustments/absolute`; the manual inventory endpoint uses the same runner path. | Confirmed | 03/23/2026 12:12:00 PM EDT |
| Resumable catalog sync batches could not enqueue their continuation jobs because the worker Lambda was missing `INTEGRATION_JOB_QUEUE_URL` in its environment and lacked `sqs:SendMessage` permission on the integration queue. | Add the integration queue URL to the worker Lambda environment in CDK and grant the worker role `sqs:SendMessage` on the integration queue so continuation enqueue remains a required, hard-failing step when batching is enabled. | Confirmed | 03/23/2026 05:55:00 PM EDT |
| Inventory reads could still never reach BigCommerce when a sync failed later in the same batch, because pending inventory targets were only flushed once at finalization after all product work completed. | Flush pending BigCommerce inventory targets incrementally during the run, flush any remaining targets before surfacing a later sync failure, and add explicit runner logs when inventory batches start and complete so both catalog syncs and inventory-only syncs show the inventory write phase clearly. | Confirmed | 03/23/2026 06:20:00 PM EDT |
| Spector inventory enrichment could still produce no BigCommerce inventory writes because PromoStandards inventory quantities were nested under `quantityAvailable -> Quantity -> value`, while the parser only handled flat numeric inventory fields and therefore derived no inventory levels or sync targets. | Parse nested PromoStandards quantity objects in both product enrichment and generic endpoint normalization so part-level and product-level inventory values are captured from shapes like `PartInventoryArray.PartInventory[].quantityAvailable.Quantity.value`. | Confirmed | 03/23/2026 06:32:00 PM EDT |
| Successful BigCommerce API snapshots could still be archived under misleading `Failed to ...` keys because the request helper reused the failure message as the telemetry action label even on `200` responses. | Derive a neutral telemetry action label from the request helper’s failure message so successful BigCommerce request snapshots and logs record the actual operation name instead of a false failure label. | Confirmed | 03/23/2026 06:45:00 PM EDT |
| GTIN values from ProductData were never reaching BigCommerce because the ETL had no GTIN field in the normalized product model and no BigCommerce payload mapping for product or variant identifiers. | Add GTIN support to normalized products and variants, extract common GTIN/UPC/EAN identifier fields from ProductData product and part records, and map them into BigCommerce product and variant `upc` payload fields. | Confirmed | 03/23/2026 08:40:00 PM EDT |
| Vendor products with valid PromoStandards media could still remain imageless in BigCommerce because the temporary existing-product inventory-only shortcut returned before media reconciliation, and raw vendor `image_url` values containing spaces were passed to BigCommerce without URL normalization. | Run vendor-managed media reconciliation for existing-product inventory-only updates too, and normalize external image URLs through URL parsing before sending them to BigCommerce’s product images API. | Confirmed | 03/23/2026 05:19:51 PM EDT |
| BigCommerce image upload failures could be silently ignored because product media reconciliation caught and only warned on product image errors after the vendor media fetch had already succeeded, leaving products marked with source-side media success even when the BigCommerce upload failed. | Stop swallowing vendor-managed media upload errors during BigCommerce product upsert and surface them as partial upsert failures so the sync records the product context but still fails the image write visibly. | Confirmed | 03/23/2026 05:30:27 PM EDT |
| ProductMedia enrichment could make unnecessary per-part follow-up calls after a successful product-level media response because the fanout logic treated any missing known part as insufficient coverage, including cases where the product-level response already returned part-specific media. | Limit ProductMedia part fanout to the intended cases only: when the vendor explicitly requires `partId` or when the product-level response contains no part-specific media coverage at all, and avoid treating the base product SKU as a part ID when variants already define the known parts. | Confirmed | 03/23/2026 05:30:27 PM EDT |
| Part-specific supplier images were only uploaded into the product gallery and never attached through BigCommerce’s variant image capability, so variants still had no direct image assignment even when PromoStandards returned part-specific media. | After variant IDs are resolved, attach only the first ranked part-specific image for each variant through BigCommerce’s product variant image endpoint while keeping the broader supplier image set in the product gallery. | Confirmed | 03/23/2026 05:30:27 PM EDT |
| BigCommerce image upload failures were not directly searchable in CloudWatch because the structured external API log only recorded the HTTP status while the returned API error body lived only in the archived snapshot payload. | Include a failed API `response_summary` in the structured external API log entry so BigCommerce image error text is searchable directly in CloudWatch without opening the archived snapshot. | Confirmed | 03/23/2026 05:41:11 PM EDT |
| PromoStandards custom endpoint URLs could be dropped for endpoints like OrderShipmentNotification because automatic mapping selection always preferred the highest discovered version, and saved mapping runtime overrides were not rehydrated back into the vendor edit payload. | Prefer manually overridden endpoint versions when resolving PromoStandards mappings, and merge saved vendor mapping runtime overrides back into the returned PromoStandards capability payload so custom endpoint URLs stay visible after reload and remain the version used at runtime. | Confirmed | 03/23/2026 05:37:09 PM EDT |
| Explicit PromoStandards service endpoint URLs that used vendor-specific version tokens like Gemline `.../Inventory/v2/GemlineInventoryService.svc` were still normalized a second time by the SOAP client because the resolver only treated exact `2.0.0` path segments as final. | Treat explicit service-style endpoint URLs as final when the path already references the endpoint and includes either a service target like `.svc`/`service` or a vendor version token like `v2`, so Gemline-style custom URLs are not appended with `/inventory/2.0.0`. | Failed | 03/24/2026 01:35:00 PM EDT |
| PromoStandards manual custom endpoints entered in the vendor UI could still be appended with derived endpoint/version segments during both targeted endpoint tests and runtime SOAP calls, because the system relied on URL-shape heuristics instead of honoring an explicit override as final. | When runtime config includes an explicit custom endpoint path or URL, treat the resolved endpoint as final in both SOAP WSDL inspection and SOAP invocation so no endpoint-name or version suffix is added. | Confirmed | 03/24/2026 02:41:00 PM EDT |
| PromoStandards vendor setup and runtime still depended on `vendor_api_url` plus mapping runtime URL overrides, which could not model vendors that use fully custom endpoint URLs per service and made manual/non-CompanyData onboarding unreliable. | Add the `vendor_endpoint_url` table, backfill legacy endpoint URLs into it, switch PromoStandards runtime and vendor testing to use stored full endpoint URLs only, detect versions from URL before WSDL, and update the vendor form to support either CompanyData discovery or manual full-URL entry per high-level endpoint. | Confirmed | 03/24/2026 06:20:00 PM EDT |
