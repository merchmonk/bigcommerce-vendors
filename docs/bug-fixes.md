| Issue | Bug Fix | Fix Status (pending, confirmed, or failed) | Fix Datetime |
|---|---|---|---|
| Lambda package size exceeded restrictions due to multiple prisma clients | Removed the local macOS Prisma client from the deploy artifact. | Confirmed | 03/21/2026 03:22:01 PM EST |
| BigCommerce category creation failed on vendor taxonomy strings that exceeded category name limits and blocked the full sync. | Prefer hierarchical vendor categories, drop invalid overlong category segments before create, and soft-skip BigCommerce `422` invalid-name category errors instead of failing the whole product sync. | Confirmed | 03/22/2026 01:40:00 PM EST |
| Catalog sync jobs stayed `ENQUEUED` after terminal BigCommerce validation failures, and cancel requests from the UI failed. | Treat BigCommerce `4xx` validation failures as non-retryable in the integration worker and update the vendor sync UI to cancel via `POST` with `action: cancel`. | Confirmed | 03/22/2026 03:35:00 PM EST |
| BigCommerce product creation failed because PromoStandards weight data was not being mapped into the outbound product payload. | Normalize product and variant weight from PromoStandards `Dimension.weight` and include `weight` in the BigCommerce product payload. | Confirmed | 03/22/2026 04:05:00 PM EST |
| BigCommerce price list upsert failed because price list records were missing `currency`. | Add `currency` to every projected price list record before upsert. | Confirmed | 03/22/2026 04:20:00 PM EST |
| PromoStandards endpoint discovery assumed normalized endpoint URL patterns, which broke vendors that expose nonstandard endpoint paths like Spector `productpriceandconfiguration` or vendor-specific service bindings. | Harvest resolved endpoint URLs from `CompanyData` service details, store them on discovered capability rows, persist them into per-mapping runtime config, and allow manual endpoint path overrides from the vendor UI. | Confirmed | 03/22/2026 08:10:00 PM EST |
| PromoStandards manual endpoint overrides had to be entered once per operation row, and targeted URI tests failed on valid endpoints when the SOAP service returned expected validation faults instead of HTTP `200`. | Group the vendor UI by endpoint and version so one manual path applies to every operation on that endpoint version, and run targeted endpoint tests through the PromoStandards WSDL-aware probe logic instead of the generic SOAP `200`-only connection test. | Confirmed | 03/22/2026 08:55:00 PM EST |
| Explicit PromoStandards endpoint override paths were still being normalized a second time by the SOAP client, causing valid paths like `/orderstatus/2.0.0` to become invalid doubled URLs such as `/orderstatus/2.0.0/orderstatusservice/2.0.0`. | Treat any already versioned endpoint path as final in the SOAP URL resolver so manual overrides and resolved service-detail URLs are not appended with the endpoint name a second time. | Failed | 03/22/2026 09:18:00 PM EST |
| ProductCompliance calls failed because the canonical mapping used the legacy operation name `getComplianceData` even though vendors expose the operation as `getCompliance`. | Correct the PromoStandards seed to use `getCompliance` and add a legacy SOAP operation alias so existing mapped vendors still call `getCompliance` at runtime. | Confirmed | 03/22/2026 09:28:00 PM EST |
| PricingAndConfiguration calls to Spector failed with `Currency ID not found` because the pricing request payload omitted the required `currency` field. | Add a default pricing `currency` request field for PromoStandards `PricingAndConfiguration` calls, with runtime config still able to override it when needed. | Confirmed | 03/22/2026 10:02:00 PM EST |
| PromoStandards PricingAndConfiguration auxiliary operations were still missing vendor-required derived fields, causing Spector pricing calls like `getDecorationColors` and `getConfigurationAndPricing` to fail even after adding `currency`. | Order pricing operations so prerequisite lookups run first, derive `locationId` and `fobId` from earlier pricing responses, and default `getConfigurationAndPricing` to `priceType=List` and `configurationType=Blank` unless runtime config overrides them. | Confirmed | 03/22/2026 10:15:00 PM EST |
| BigCommerce product updates failed with duplicate custom field errors like `vendor_endpoint=ProductData already exists` because product `PUT` payloads resent custom fields that BigCommerce treats as additive. | Keep product custom fields on create, but omit them from BigCommerce product update payloads so existing custom fields are preserved instead of duplicated. | Confirmed | 03/22/2026 10:33:00 PM EST |
| BigCommerce price-list pricing could silently fail to attach on first sync because newly created variant IDs were discarded and the sync relied on an immediate variant re-list that may not include fresh variants yet. | Preserve variant IDs returned by BigCommerce create/update responses and use them directly when building price-list records, merging in any later re-list results instead of depending on the re-list alone. | Confirmed | 03/22/2026 10:52:00 PM EST |
| Parent products with option variants could still show no base pricing in BigCommerce because the sync only wrote price-list records for option variants and never attached the fallback base price to the product `base_variant_id`. | Always map the BigCommerce `base_variant_id`, keep fallback `price` and `cost_price` on the parent product payload, and add a base-variant price-list record for variant products using the projected product fallback price. | Confirmed | 03/22/2026 11:04:00 PM EST |
| BigCommerce modifier updates failed with `An option or modifier must have not more than one default` because modifier `PUT` payloads resent `is_default` flags while BigCommerce still retained an existing default option value. | Keep `is_default` on modifier creates, but omit it from modifier update payloads so updates don’t introduce a second default. | Confirmed | 03/22/2026 11:14:00 PM EST |
| BigCommerce modifier updates could still fail with duplicate option-label errors like `The option label: "6" value is already used on this option` because modifier `PUT` does not reliably replace existing option values. | When a managed modifier update returns a duplicate/validation-style `422`, delete that modifier and recreate it instead of failing the full sync. | Confirmed | 03/22/2026 11:24:00 PM EST |
| Vendor row product stats could stay at `0 synced / 0 active` even after a BigCommerce product already existed in the storefront, because later sync failures prevented `vendor_product_map` from being written. | Propagate partial BigCommerce upsert details when the product record already exists, and persist `vendor_product_map` plus `records_written` from the runner even if a later post-upsert step fails. | Confirmed | 03/22/2026 11:39:00 PM EST |
| BigCommerce variant creation could fail with `422` after product options were created because the sync still posted variants using `option_display_name` and `label` instead of the BigCommerce option ID and option-value ID pairs required by the standalone variants endpoint. | Re-read product options after ensuring they exist, build an option/value ID lookup, and send variant `option_values` using `{ option_id, id }` when those IDs are available. | Confirmed | 03/22/2026 11:58:00 PM EST |
| BigCommerce variant creation could still fail with `409 Variant with the same option values set exists` when vendor parts normalized to duplicate option combinations or when a matching variant already existed by combination during a retry/update path. | Make normalized variant option combinations unique by appending a `Part` option to colliding variants, and when variant create hits the duplicate-combination `409`, refresh existing variants and update the matching variant instead of failing the sync. | Confirmed | 03/23/2026 12:12:00 AM EST |
| Catalog syncs could fail too late on missing-data products, either halting a run after several successful writes or wasting effort when early products were all blocked by enrichment failures. | Let blocked products be reported and skipped once the run proves healthy, but add two safety rails in the runner: fail fast if the first 2 evaluated products both fail enrichment, and fail the job once at least 100 products have been attempted and 50% or more are blocked. | Confirmed | 03/23/2026 12:29:00 AM EST |
| BigCommerce product bulk pricing rule creation failed with `422 Bulk pricing rule property quantityMax should be set` when vendor tiers had an open-ended upper bound. | Always send `quantity_max` for catalog bulk pricing rules, defaulting open-ended tiers to `0` instead of omitting the field. | Failed | 03/23/2026 12:41:00 AM EST |
| BigCommerce product create still failed with `422 Bulk pricing rule property quantityMax should be set` because bulk pricing rules were also being sent inline on the base product payload before the dedicated bulk-pricing sync ran. | Stop sending inline `bulk_pricing_rules` on the base product create/update payload and let the dedicated post-upsert bulk-pricing sync own all catalog bulk pricing writes. | Confirmed | 03/23/2026 12:49:00 AM EST |
| BigCommerce bulk pricing rule creation could still fail with `409 ... quantity range ... must not overlap` because multiple vendor bulk tiers with only `quantityMin` were being sent as overlapping open-ended ranges. | Normalize catalog bulk pricing ranges before write so each tier ends just before the next tier starts, and only the final tier remains open-ended. | Confirmed | 03/23/2026 12:56:00 AM EST |
| BigCommerce price list record upserts failed with `400 ... type missing / amount missing` because nested `bulk_pricing_tiers` were still being sent in the wrong shape. | Change price list bulk tiers to use BigCommerce’s expected `{ quantity_min, quantity_max?, type: 'price', amount }` payload shape before writing price list records. | Confirmed | 03/23/2026 01:03:00 AM EST |
| Product inventory was landing in BigCommerce as `0` because inventory enrichment only stored a single product-level total and never mapped part-level inventory quantities back onto variants before variant sync. | Parse inventory responses into both a total and a per-part map, then apply matching quantities onto normalized variants so variant inventory writes use real vendor stock instead of falling back to `0`. | Confirmed | 03/23/2026 01:17:00 AM EST |
| Variant inventory could still be lost during product merges because duplicate variants were de-duped by SKU using a first-write-wins filter that discarded later non-empty fields. | Merge duplicate variants field-by-field during product consolidation in the normalizer and runner so later inventory values are preserved instead of dropped. | Confirmed | 03/23/2026 01:24:00 AM EST |
| Updated products could still send `inventory_level: 0` to BigCommerce because inventory enrichment defaulted `partId` to the product SKU or product ID, so Spector inventory calls were made with invalid part IDs like `BB196` instead of the real part IDs such as `00BB1960BLK`. | Stop sending the default fallback `partId` on `Inventory` endpoint calls and let the vendor return product-level or part-array inventory data, which is then matched back onto variants from the response payload. | Confirmed | 03/23/2026 02:42:00 AM EST |
| Existing products needed a temporary low-risk sync mode because repeated full updates were still causing instability, while the immediate business need was to keep storefront inventory current. | Add a temporary existing-product shortcut that skips the general BigCommerce update path and only updates inventory for already-synced products; new products still go through the normal create flow. | Confirmed | 03/23/2026 02:51:00 AM EST |
| Flat vendor category lists could still assign noisy non-taxonomy tags like `MADE IN CHINA` and `Branding solutions` to BigCommerce products because the fallback category normalizer only de-duped values when no hierarchical category path existed. | Keep the existing hierarchy-first behavior, but filter flat category values that start with `made in` and the tag `branding solutions` before assigning fallback BigCommerce categories. | Confirmed | 03/23/2026 01:36:00 AM EST |
| Catalog syncs could stop creating new products and then appear to loop over the same SKUs because `ProductData` discovery returned multiple `productId + partId` references for the same product, and the runner upserted each resolved full product again on later part references. | Track processed vendor product IDs within a sync run and skip duplicate product assemblies/upserts when later `ProductData` references resolve to a vendor product that was already processed earlier in the same run. | Failed | 03/23/2026 01:49:00 AM EST |
| Catalog syncs could still stall for a long time with no new products added because the duplicate-reference guard only skipped repeated writes after fetch, while the runner still called `getProduct` for every duplicate `productId + partId` reference. | Skip duplicate `ProductData` references before the vendor fetch by only calling `getProduct` once per `productId` within a sync run, with the later duplicate-product guard left in place as a safety backstop. | Confirmed | 03/23/2026 02:16:00 AM EST |
| Catalog syncs could still hang after a product image upload because the BigCommerce media sync still called the product video API even when no videos were desired, and the shared BigCommerce request helper had no timeout to fail a stalled request. | Keep the catalog media sync image-only for now by skipping BigCommerce video calls entirely, and add a shared timeout to `requestJson()` so a stalled BigCommerce request aborts instead of freezing the whole sync run. | Confirmed | 03/23/2026 02:28:00 AM EST |
| Cancelled or crashed catalog syncs could leave older `etl_sync_runs` rows stuck in `RUNNING`, so the sync panel showed multiple active-looking runs and made cancellation state confusing even when the latest integration job was already cancelled. | Reconcile stale catalog sync runs by marking older `PENDING/RUNNING` rows as failed once a newer active run exists, or when no active catalog sync job exists for the vendor, and run that cleanup before new syncs start and before the sync panel loads runs. | Confirmed | 03/23/2026 02:03:00 AM EST |
| The vendor sync panel repeatedly refetched sync and mapping APIs while idle, creating a near-continuous request loop in the UI. | Remove SWR auto-refresh and focus/reconnect revalidation from the vendor sync panel so data only reloads on page refresh or after explicit run/cancel actions. | Confirmed | 03/22/2026 08:32:00 PM EST |
| Vendor row action dropdowns on the vendors table rendered inside the table overflow container, so opening the ellipsis menu could create a small scroll box and clip actions like Sync now or Edit out of view. | Render the shared row action menu through a portal on `document.body` and anchor it with fixed viewport positioning so the dropdown overlays the table instead of being clipped inside it. | Confirmed | 03/22/2026 04:51:53 PM EDT |
| Inventory-only syncs could keep product and variant inventory at `0` because existing-product inventory updates matched variants by BigCommerce SKU only and defaulted missing inventory writes to zero. | Match existing variants by normalized SKU or `Part` option label, and omit inventory writes when no vendor inventory value is present instead of forcing `0`. | Confirmed | 03/23/2026 10:35:00 AM EDT |
| Existing-product inventory-only syncs could still leave inventory nonfunctional because the update branch wrote counts but never repaired the BigCommerce product’s `inventory_tracking` mode. | When running the inventory-only update path, explicitly set `inventory_tracking` to `product` for simple products and `variant` for variant products before applying inventory values. | Confirmed | 03/23/2026 10:49:00 AM EDT |
| BigCommerce product image descriptions were missing variant, location, and decoration associations, and media parsing could pollute location data with unrelated numeric/address fields. | Tighten PromoStandards media identifier extraction, preserve location and decoration names on normalized media assets, and include both IDs and resolved names in the `mm_media` marker stored on uploaded BigCommerce images. | Confirmed | 03/23/2026 10:35:00 AM EDT |
| PromoStandards `availableLocations` data from Spector exposed a supplier address rather than decorator placement options, which made downstream decoration-location data misleading. | Stop projecting `availableLocations` into the designer contract and rely on `Configuration.LocationArray.Location` for decoration placement data instead. | Confirmed | 03/23/2026 10:35:00 AM EDT |
| Full catalog syncs could time out at the 15-minute Lambda limit after only partially processing a large vendor catalog, requiring manual reruns to continue the import. | Split ProductData-driven catalog syncs into resumable batches using a continuation offset, preserve the original discovery checkpoint across batches, and automatically enqueue the next batch only after the current batch completes successfully. | Confirmed | 03/23/2026 11:40:00 AM EDT |
| BigCommerce inventory quantities could stay at `0` because the sync was still writing quantities through Catalog product and variant `PUT` calls instead of the Inventory API path BigCommerce now expects. | Stop sending quantity through Catalog product and variant payloads, build inventory sync targets during product upsert, and flush batch inventory at the end of each sync run through `PUT /v3/inventory/adjustments/absolute`; the manual inventory endpoint uses the same runner path. | Confirmed | 03/23/2026 12:12:00 PM EDT |
| Resumable catalog sync batches could not enqueue their continuation jobs because the worker Lambda was missing `INTEGRATION_JOB_QUEUE_URL` in its environment and lacked `sqs:SendMessage` permission on the integration queue. | Add the integration queue URL to the worker Lambda environment in CDK and grant the worker role `sqs:SendMessage` on the integration queue so continuation enqueue remains a required, hard-failing step when batching is enabled. | Confirmed | 03/23/2026 05:55:00 PM EDT |
| Inventory reads could still never reach BigCommerce when a sync failed later in the same batch, because pending inventory targets were only flushed once at finalization after all product work completed. | Flush pending BigCommerce inventory targets incrementally during the run, flush any remaining targets before surfacing a later sync failure, and add explicit runner logs when inventory batches start and complete so both catalog syncs and inventory-only syncs show the inventory write phase clearly. | Confirmed | 03/23/2026 06:20:00 PM EDT |
| Spector inventory enrichment could still produce no BigCommerce inventory writes because PromoStandards inventory quantities were nested under `quantityAvailable -> Quantity -> value`, while the parser only handled flat numeric inventory fields and therefore derived no inventory levels or sync targets. | Parse nested PromoStandards quantity objects in both product enrichment and generic endpoint normalization so part-level and product-level inventory values are captured from shapes like `PartInventoryArray.PartInventory[].quantityAvailable.Quantity.value`. | Confirmed | 03/23/2026 06:32:00 PM EDT |
| Successful BigCommerce API snapshots could still be archived under misleading `Failed to ...` keys because the request helper reused the failure message as the telemetry action label even on `200` responses. | Derive a neutral telemetry action label from the request helper’s failure message so successful BigCommerce request snapshots and logs record the actual operation name instead of a false failure label. | Confirmed | 03/23/2026 06:45:00 PM EDT |
| GTIN values from ProductData were never reaching BigCommerce because the ETL had no GTIN field in the normalized product model and no BigCommerce payload mapping for product or variant identifiers. | Add GTIN support to normalized products and variants, extract common GTIN/UPC/EAN identifier fields from ProductData product and part records, and map them into BigCommerce product and variant `upc` payload fields. | Confirmed | 03/23/2026 08:40:00 PM EDT |
| Vendor products with valid PromoStandards media could still remain imageless in BigCommerce because the temporary existing-product inventory-only shortcut returned before media reconciliation, and raw vendor `image_url` values containing spaces were passed to BigCommerce without URL normalization. | Run vendor-managed media reconciliation for existing-product inventory-only updates too, and normalize external image URLs through URL parsing before sending them to BigCommerce’s product images API. | Confirmed | 03/23/2026 05:19:51 PM EDT |
| BigCommerce image upload failures could be silently ignored because product media reconciliation caught and only warned on product image errors after the vendor media fetch had already succeeded, leaving products marked with source-side media success even when the BigCommerce upload failed. | Stop swallowing vendor-managed media upload errors during BigCommerce product upsert and surface them as partial upsert failures so the sync records the product context but still fails the image write visibly. | Confirmed | 03/23/2026 05:30:27 PM EDT |
| ProductMedia enrichment could make unnecessary per-part follow-up calls after a successful product-level media response because the fanout logic treated any missing known part as insufficient coverage, including cases where the product-level response already returned part-specific media. | Limit ProductMedia part fanout to the intended cases only: when the vendor explicitly requires `partId` or when the product-level response contains no part-specific media coverage at all, and avoid treating the base product SKU as a part ID when variants already define the known parts. | Confirmed | 03/23/2026 05:30:27 PM EDT |
| Part-specific supplier images were only uploaded into the product gallery and never attached through BigCommerce’s variant image capability, so variants still had no direct image assignment even when PromoStandards returned part-specific media. | After variant IDs are resolved, attach only the first ranked part-specific image for each variant through BigCommerce’s product variant image endpoint while keeping the broader supplier image set in the product gallery. | Confirmed | 03/23/2026 05:30:27 PM EDT |
| BigCommerce image upload failures were not directly searchable in CloudWatch because the structured external API log only recorded the HTTP status while the returned API error body lived only in the archived snapshot payload. | Include a failed API `response_summary` in the structured external API log entry so BigCommerce image error text is searchable directly in CloudWatch without opening the archived snapshot. | Confirmed | 03/23/2026 05:41:11 PM EDT |
| PromoStandards custom endpoint URLs could be dropped for endpoints like OrderShipmentNotification because automatic mapping selection always preferred the highest discovered version, and saved mapping runtime overrides were not rehydrated back into the vendor edit payload. | Prefer manually overridden endpoint versions when resolving PromoStandards mappings, and merge saved vendor mapping runtime overrides back into the returned PromoStandards capability payload so custom endpoint URLs stay visible after reload and remain the version used at runtime. | Confirmed | 03/23/2026 05:37:09 PM EDT |
| Explicit PromoStandards service endpoint URLs that used vendor-specific version tokens like Gemline `.../Inventory/v2/GemlineInventoryService.svc` were still normalized a second time by the SOAP client because the resolver only treated exact `2.0.0` path segments as final. | Treat explicit service-style endpoint URLs as final when the path already references the endpoint and includes either a service target like `.svc`/`service` or a vendor version token like `v2`, so Gemline-style custom URLs are not appended with `/inventory/2.0.0`. | Failed | 03/24/2026 01:35:00 PM EDT |
| PromoStandards manual custom endpoints entered in the vendor UI could still be appended with derived endpoint/version segments during both targeted endpoint tests and runtime SOAP calls, because the system relied on URL-shape heuristics instead of honoring an explicit override as final. | When runtime config includes an explicit custom endpoint path or URL, treat the resolved endpoint as final in both SOAP WSDL inspection and SOAP invocation so no endpoint-name or version suffix is added. | Confirmed | 03/24/2026 02:41:00 PM EDT |
| PromoStandards vendor setup and runtime still depended on `vendor_api_url` plus mapping runtime URL overrides, which could not model vendors that use fully custom endpoint URLs per service and made manual/non-CompanyData onboarding unreliable. | Add the `vendor_endpoint_url` table, backfill legacy endpoint URLs into it, switch PromoStandards runtime and vendor testing to use stored full endpoint URLs only, detect versions from URL before WSDL, and update the vendor form to support either CompanyData discovery or manual full-URL entry per high-level endpoint. | Confirmed | 03/24/2026 06:20:00 PM EDT |
| Resumable full catalog sync continuations could fall back to incremental ProductData discovery after the first 15-product batch because continuation jobs treated an intentionally `null` full-sync checkpoint like a missing value, and stale continuation retries with no discovered ProductData references could then crash while reading `getProductConfig.mapping`. | Preserve `null` as the full-sync continuation checkpoint so continuation jobs stay on `getProductSellable`, delay continuation queue delivery briefly until the parent job releases the vendor lock, and guard the continuation path against missing `getProductConfig` when no follow-up ProductData references are discovered. | Confirmed | 03/31/2026 11:28:54 PM EDT |
| Resumable catalog sync batching could trigger AWS Lambda recursive loop detection because the integration worker both consumed the integration SQS queue and enqueued continuation jobs back onto that same queue. | Persist continuation state on successful sync runs, move continuation job submission into a separate EventBridge-driven catalog continuation scheduler Lambda, and remove queue send permissions from the integration worker so batching no longer self-recurses through the worker event source. | Pending | 04/01/2026 10:32:00 AM EDT |
| BigCommerce product image sync could fail with `422 The field 'image_url' is invalid` when supplier media URLs were syntactically valid but the remote asset returned `404` or non-image content, because the sync only normalized the URL string before posting it to BigCommerce. | Preflight remote image URLs with `HEAD` plus `GET` fallback, skip unreachable or non-image assets before BigCommerce upload, and reuse the same validation for variant image attachments. | Failed | 04/01/2026 03:58:06 PM EDT |
| BigCommerce vendor-managed product images could be deleted and recreated on every rerun because the reconciler created the desired gallery images first and then deleted every existing vendor-managed image instead of matching the stored `mm_media` marker back to the desired image set. | Compare existing and desired product gallery images by parsed `mm_media` metadata plus thumbnail state, create only missing images, and delete stale vendor-managed images only after the desired reachable image set has been satisfied so unchanged galleries remain stable across reruns. | Failed | 04/01/2026 04:47:00 PM EDT |
| Full vendor sync traces could collapse under a single job-wide correlation ID, making all product-level vendor and BigCommerce snapshots for a run appear under the same correlation instead of a product-specific child correlation. | Keep the job-level correlation for the run, but fork a product-scoped child correlation in the runner for each ProductData reference and direct product upsert so external API traces and snapshots remain unique to the product being processed. | Confirmed | 04/01/2026 04:54:00 PM EDT |
| Synced BigCommerce products were only internally associated to a vendor by `vendor_id`, which made it difficult for operators to find all products for a vendor by name during catalog review. | Add a shared internal `vendor_name` modifier alongside the existing `vendor_id` modifier on both full upserts and inventory-only existing-product syncs so vendor association remains searchable without exposing the vendor name through storefront search keywords. | Confirmed | 04/01/2026 06:07:00 PM EDT |
| BigCommerce modifier creation could fail with `422` duplicate option-label errors when PromoStandards decoration labels differed only by casing, because the sync forwarded case-variant duplicates like `Centered On Body` and `Centered on Body` into the same modifier payload. | Normalize modifier option labels for duplicate detection before modifier create/update payloads are built, collapse case-only duplicate values into one BigCommerce option entry, and preserve the first non-empty adjuster for that surviving value. | Confirmed | 04/01/2026 06:28:00 PM EDT |
| Vendor sync workers could run out of memory on large catalogs because the runner retained full endpoint/status/retry arrays for the entire sync and oversized supplier images were buffered and re-encoded in-memory while `sharp` kept its default cache enabled. | Store capped sync-detail samples plus counts instead of full per-product arrays, cap oversized image staging downloads before buffering, and disable the `sharp` cache in the Lambda image staging path to keep memory bounded during long-running syncs. | Confirmed | 04/02/2026 12:15:00 PM EDT |
| BigCommerce modifier creation could fail with an empty-body `500` on otherwise valid shared modifiers like `duplicate=false`, causing the sync to abort even when the modifier was created server-side or the server error was transient. | On modifier create `5xx` responses, re-list the product modifiers to detect ambiguous success, reconcile through the existing modifier update path when the modifier now exists, and otherwise retry modifier creation with backoff before failing the sync. | Confirmed | 04/02/2026 12:46:00 PM EDT |
| Operators could not resume a failed full vendor sync from the last persisted checkpoint in the vendor UI, forcing manual API calls with a session cookie just to continue from the last processed reference index. | Expose the latest resumable `processed_references` checkpoint from the vendor sync API and add a vendor-page resume button that reruns the full sync from that saved `start_reference_index`. | Pending | 04/02/2026 04:58:00 PM EDT |
| BigCommerce inventory sync could fail before any stock adjustment when the extra `/v3/inventory/locations` lookup returned `504`, even though the store was using the default inventory location. | Stop listing inventory locations during sync and write absolute inventory adjustments directly against the default BigCommerce location `1`. | Pending | 04/02/2026 04:58:00 PM EDT |
| BigCommerce managed modifiers could churn on every rerun because the sync always attempted a modifier `PUT` for any existing modifier name, causing BigCommerce to answer `422` and forcing the fallback `delete -> create` path even when the modifier values were already correct. | Compare existing modifier option labels and adjusters to the desired payload first, and skip modifier writes entirely when the current BigCommerce modifier already matches the managed values. | Pending | 04/02/2026 05:21:00 PM EDT |
| BigCommerce vendor-managed product images could still churn on rerun when older `mm_media` markers stored only stable IDs while newer desired markers also included resolved location or decoration names, because the matcher treated the richer marker payload as a different image identity. | Match vendor-managed image signatures by normalized URL, part ID, and stable location/decoration IDs first, falling back to normalized names only when IDs are absent, so legacy markers and current markers resolve to the same image identity. | Pending | 04/02/2026 05:39:00 PM EDT |
| Duplicate BigCommerce product detection could miss existing products or create false duplicate checks because the sync searched by vendor SKU and product name instead of the product GTIN, even though vendor SKUs and names are not stable across suppliers. | Restrict duplicate candidate discovery to exact GTIN (`upc`) matches only, and skip duplicate lookup entirely when the source product does not include a GTIN. | Pending | 04/02/2026 06:11:00 PM EDT |
| Deferred BigCommerce related-product linking could spam repeated `GET /related-products` calls and never settle cleanly during batched syncs, because the runner queued one pending link per target and then re-listed source related products for each pending row even when the endpoint returned `404`. | Temporarily disable BigCommerce related-product link resolution during sync, stop queueing pending related-product rows, and persist supplier related vendor product IDs onto the product as a comma-separated `related_vendor_product_ids` custom field for later manual or redesigned linking. | Failed | 04/02/2026 06:42:00 PM EDT |
| BigCommerce modifier updates still recreated modifiers on real value changes because the sync treated the parent modifier `PUT` endpoint as the place to replace option values instead of reconciling individual modifier values. | Stop using parent modifier `PUT` for existing modifiers, list current modifier values through `/modifiers/{modifier_id}/values`, and reconcile value changes in place through the dedicated modifier-value create/update/delete endpoints. | Pending | 04/02/2026 07:04:00 PM EDT |
| BigCommerce product creates could still fail with `409 Sku ... is not unique` after GTIN-only duplicate detection, because the initial product `POST` embedded child variants whose SKUs collided elsewhere and missing-GTIN reruns had no persisted-map fallback before attempting a fresh create. | Stop embedding variants in the initial BigCommerce product create payload, reconcile all variants through the dedicated variant endpoints, retry duplicate-SKU create conflicts with vendor-scoped fallback SKUs only after BigCommerce rejects the request, and reuse `vendor_product_map` for missing-GTIN reruns before attempting a new product create. | Failed | 04/02/2026 02:44:00 PM EDT |
| BigCommerce product creates could still fail with `409 The product sku is a duplicate` after the duplicate-SKU retry path shipped, because the retry matcher only recognized BigCommerce's older `Sku ... is not unique` wording and skipped the same retry flow for the alternate duplicate response body. | Treat both `not unique` and `is a duplicate` BigCommerce SKU validation messages as duplicate-SKU conflicts so product and variant create retries consistently fall back to vendor-scoped SKUs regardless of which 409 wording BigCommerce returns. | Confirmed | 04/02/2026 03:01:38 PM EDT |
| BigCommerce price-list sync could fail with `422 The variant with id ... does not exist` immediately after product upsert, because the sync still tried to price the product `base_variant_id` even when BigCommerce had already replaced it with explicit option-bearing variants and only those real variant IDs remained valid. | Stop mapping or pricing the base variant for products that have option-bearing variants, and only send price-list records for the explicit variant IDs returned by the post-sync variant reconciliation. | Confirmed | 04/02/2026 03:14:32 PM EDT |
| BigCommerce CSV upload prep for vendor `10` produced import files larger than the platform's 20 MB upload limit, blocking manual CSV import even though the product mapping was correct. | Chunk the generated BigCommerce product, SKU, and metafield exports into numbered CSV parts capped at 19 MB or less with the header row repeated in every chunk, and emit a manifest describing the import order. | Confirmed | 04/03/2026 02:24:00 PM EDT |
| BigCommerce rejected the vendor `10` product import chunks with `"Item" column is absent or empty` because the generated file was modeled after the bulk-edit export schema (`Item Type`, `Product/SKU/Rule`) instead of the current v3 product import template (`Item`, `Product/Variant`). | Rebuild the upload CSV against the v3 product import template so parent rows use `Item=Product`, variant rows use `Item=Variant`, and the initial import no longer depends on a separate SKU CSV. | Confirmed | 04/03/2026 03:02:00 PM EDT |
| BigCommerce product image sync was dropping important PromoStandards `ProductMedia` metadata from the vendor-managed image description marker and choosing product/variant primary images by position instead of the `Primary` class type. | Parse structured `ClassTypeArray` entries, persist richer `mm_media` metadata including product/class/size/color/timestamp fields plus resolved location and decoration arrays, and choose both the product thumbnail and per-part variant image from the first image marked `Primary` when available. | Confirmed | 04/03/2026 04:02:00 PM EDT |
| PromoStandards enrichment calls could return HTTP `200` with a nested `ErrorMessage { code, description }`, which let products continue without required pricing or location data instead of being blocked and counted as enrichment failures. | Detect nested PromoStandards `ErrorMessage` payloads on non-media enrichment endpoints, surface the supplier code and description in endpoint results and product gating reasons, block those products from sync, and let the existing blocked-product stop rules halt the run when the error is widespread. | Confirmed | 04/03/2026 05:02:00 PM EDT |
| PromoStandards `PricingAndConfiguration` responses were only partially persisted into BigCommerce, so purchase quantity constraints and operation payloads like decoration colors, FOB support, and full configuration details were missing from the synced product record. | Map part-tier purchase quantity constraints onto the BigCommerce product and variants during full sync, normalize the missing `PricingAndConfiguration` fields including decoration colors and richer FOB metadata, and persist the full operation outputs into private `pricing_configuration_*` product metafields without changing the inventory-only sync path. | Confirmed | 04/03/2026 06:05:00 PM EDT |
| BigCommerce category sync could create duplicate taxonomy branches like multiple `Backpacks` because vendor category names were matched and created using inconsistent casing and singular/plural forms. | Normalize each category segment to title case, pluralize it with `pluralize`, and search existing BigCommerce categories using that normalized plural form before creating any new category nodes. | Confirmed | 04/03/2026 06:19:00 PM EDT |
| Sell pricing was being calculated as additive markup (`cost * 1.3`) even though the configured percentage represents target gross margin, so a 30% setting produced underpriced products instead of charging `cost / 0.7`. | Change the shared sell-price helper to use margin-based pricing `cost / (1 - percent)` with invalid `>= 100%` values guarded, and update the pricing projection and product contract expectations that depend on that helper. | Confirmed | 04/03/2026 06:28:00 PM EDT |
| Supplier product titles could sync into BigCommerce with raw HTML entity encodings like `&quot;` and `&#174;`, leaving storefront and admin product names hard to read. | Decode common named and numeric HTML entities during product-name normalization for both generic endpoint ingestion and `ProductData/getProduct` normalization before products are written downstream. | Confirmed | 04/03/2026 06:37:00 PM EDT |
| Vendor `10` BigCommerce sync pricing could choose the wrong PromoStandards price family and only write one price list because merged pricing tiers did not retain `Net Blank` vs `Net Decorated` identity. | Preserve `priceType` and `configurationType` on each normalized pricing tier, route vendor `10` price list `1` to marked-up `Net Decorated` with `Net` fallback, and route price list `2` to raw `Net Blank` records when that family is available. | Failed | 04/03/2026 09:10:00 PM EDT |
| PromoStandards BigCommerce sync pricing could choose the wrong price family and only write one price list because merged pricing tiers did not retain `Net Blank` vs `Net Decorated` identity, and the initial fix incorrectly scoped the routing to vendor `10` only. | Preserve `priceType` and `configurationType` on each normalized pricing tier, route price list `1` to marked-up `Net Decorated` with `Net` fallback for all PromoStandards vendors, and route price list `2` to raw `Net Blank` records when that family is available. | Confirmed | 04/03/2026 09:25:00 PM EDT |
| BigCommerce sync was using supplier product and part identifiers as storefront SKUs, which made the managed SKU space vendor-dependent and left the dedicated MPN fields unused on both parent products and variants. | Write supplier `vendor_product_id` / part identity to BigCommerce `mpn`, generate internal managed product SKUs for parent products and managed hashed SKUs for variants, and keep the inventory-only sync path unchanged. | Confirmed | 04/03/2026 02:28:42 PM EDT |
| Managed variant SKUs were opaque hashes, and the inventory-only existing-product path could still rely on BigCommerce storefront SKUs instead of supplier identity when resolving variant inventory targets. | Build variant SKUs from the managed parent product SKU plus the first three letters of the preferred variant label with deterministic collision handling, and resolve inventory-only existing variants through supplier identity keys including `mpn` without changing the inventory-only retrieval behavior. | Confirmed | 04/03/2026 02:37:08 PM EDT |
| PromoStandards `ProductData.getProduct` fields beyond the small normalized subset were being dropped before BigCommerce sync, so attributes like marketing points, price groups, FOB points, part packaging, compliance flags, NMFC data, and other product-detail fields were not retained anywhere in BigCommerce. | Normalize the full structured `ProductData.getProduct` payload into `product_data`, persist it as private `product_data_*` product metafields alongside the existing first-class mappings, keep `primaryImageURL` mapped to the product image input, and extend metafield cleanup to manage both `pricing_configuration_*` and `product_data_*` families. | Confirmed | 04/03/2026 02:51:24 PM EDT |
| BigCommerce product image descriptions could exceed the 255-character limit because the vendor-managed marker still embedded verbose PromoStandards media metadata and the legacy `mm_media:` prefix in both the live sync and import/export paths. | Shrink vendor-managed image descriptions to plain JSON containing only `partId` and `classTypes`, remove the `mm_media:` prefix, keep richer media data in the existing app-only product metafields, and preserve backward-compatible image matching so legacy descriptions do not churn synced images. | Confirmed | 04/03/2026 03:29:26 PM EDT |
| Vendor `10` CSV prep had drifted behind the current BigCommerce API sync, so the manual import files were missing the richer `mm_media` marker, private `pricing_configuration_*` product metafields, updated price-family routing, and a matching manual price-list append file. | Rebuild the PCNA CSV path to preserve PromoStandards price-family metadata and decoration defaults during normalization, emit `v3` `Image` rows with the same enriched `mm_media` payload the API writes, replay the private `pricing_configuration_*` product metafields, append `Default` and `Blanks` price-list rows in the current export shape, and write a discrepancy report for any source fields or API-only data the CSV flow still cannot cover. | Confirmed | 04/03/2026 01:05:00 PM EDT |
| The manual PCNA price-list export still silently capped bulk pricing to four `Bulk Pricing` column groups even though BigCommerce and the API sync both support more tiers per variant. | Remove the hardcoded 4-slot CSV limit, size the price-list import headers dynamically to the highest projected tier count, and keep the API path unchanged because it already sends the full `bulk_pricing_tiers` array. | Confirmed | 04/03/2026 01:42:00 PM EDT |
| BigCommerce v3 product CSV import rejected every row with `\"Custom Fields\" has an invalid JSON format` because the exporter still wrote the legacy semicolon-delimited bulk-edit string into the v3 `Custom Fields` column. | Serialize `Custom Fields` as a JSON array of `{ name, value }` objects in the v3 product import exporter and regenerate the vendor `10` CSV product parts. | Confirmed | 04/03/2026 02:06:00 PM EDT |
| The vendor `10` CSV export was still writing supplier style and part identifiers into the BigCommerce `SKU` columns, even though the live app had already switched to internal managed MM SKUs with supplier identity stored in `mpn`. | Reuse the managed product and variant SKU projection in the v3 product CSV, metafield CSV, and manual price-list append flow, and replace legacy supplier-SKU rows on regeneration so the CSV import path matches the current BigCommerce API sync contract. | Confirmed | 04/03/2026 03:25:12 PM EDT |
| Vendor `10` manual import prep was still tied to the flattened CSV source files, so swatch hex data, richer private `product_data_*` metafields, and the current JSON-only image marker contract were missing or degraded compared with the live PromoStandards sync. | Prefer the vendor XML endpoint dumps when available, stream and normalize them into the existing import builder, preserve per-part color hex for swatch options, emit plain JSON image descriptions, and keep reporting the remaining category-ID lookup gap where the existing BigCommerce export has no match. | Confirmed | 04/03/2026 09:55:00 PM EDT |
| BigCommerce v3 product CSV import still rejected many vendor `10` rows with duplicate product URLs and invalid image URLs because the exporter forced custom slugs that collided with existing storefront products and passed vendor media URLs through without URL-encoding spaces. | Leave `Product URL` blank in the v3 CSV to match the API sync behavior, and normalize every variant and image URL through URL parsing so vendor asset paths with spaces are emitted as valid encoded import URLs. | Confirmed | 04/06/2026 02:32:00 PM EDT |
| PromoStandards `PricingAndConfiguration` supplier calls could still return `Error 120 required: locationId/fobId` even when those values were derived, because the SOAP builder serialized request fields in object insertion order instead of the schema-required XSD sequence for PricingAndConfiguration requests. | Add schema field-order metadata for built-in PricingAndConfiguration SOAP operations and serialize request children in that order so derived fields like `locationId`, `currency`, `fobId`, `priceType`, and `configurationType` appear where the vendor service expects them. | Confirmed | 04/06/2026 10:24:52 AM EDT |
| Existing BigCommerce products synced from the full app flow were still taking the temporary inventory-only shortcut, so managed SKUs/MPNs were never backfilled on update and `product_designer_defaults` never received the richer media payload. | Restrict the inventory-only shortcut to the manual inventory sync path, project managed product and variant SKUs into the contract payload for full syncs, and restore structured image-group projection into `product_designer_defaults.media`. | Confirmed | 04/06/2026 10:59:06 AM EDT |
| The manual inventory-only sync path was still reconciling modifiers, related vendor custom fields, and vendor-managed media for existing products, causing repeated BigCommerce catalog reads/writes unrelated to inventory and making the sync look like a partial product update. | Keep the inventory-only existing-product branch limited to inventory tracking setup, existing variant ID resolution, and the later Inventory API batch update, skipping modifier, custom-field, contract, and image reconciliation entirely. | Confirmed | 04/06/2026 11:04:40 AM EDT |
| PromoStandards decoration modifiers could include raw numeric `locationId` values like `1000190` in the BigCommerce `Decoration Location` modifier when later pricing payloads referenced only `locationId` and the blueprint extractor treated ids as display labels. | Resolve modifier blueprint and charge locations through a shared `locationId -> locationName` lookup built across pricing payloads, and only emit human-readable location names into the `Decoration Location` modifier options. | Confirmed | 04/06/2026 11:31:21 AM EDT |
| BigCommerce related-product links stopped syncing after the temporary disable workaround, leaving `related_vendor_product_ids` on the product but never creating actual related-product associations even when both products already existed in BigCommerce. | Re-enable deferred related-product link resolution, keep identity matching on `vendor_product_id` rather than managed storefront SKU, and batch target links per source product so sync creates one BigCommerce related-products write per source instead of repeating the same source lookup and re-list cycle for every target. | Confirmed | 04/06/2026 11:37:13 AM EDT |
| Variant contract metafield sync could update and then delete the same BigCommerce variant metafield during one run because the variant-id lookup now stores alias keys like managed SKU, part id, source SKU, and MPN, while the contract sync still iterated by key instead of unique variant id. | Dedupe variant contract-metafield reconciliation by unique `variantId`, resolve the override payload from the canonical projected variant SKU once, and only upsert or delete the metafield once per real variant. | Confirmed | 04/06/2026 12:07:00 PM EDT |
| BigCommerce related-product sync and designer runtime reads could 404 on every product because the app called a non-existent V3 `/catalog/products/{id}/related-products` subresource instead of using the documented product-level `related_products` field. | Read and write related-product ids through the V2 product `related_products` field, keep `related_vendor_product_ids` as the internal vendor-reference custom field only, and stop calling the 404ing V3 related-products subresource. | Confirmed | 04/06/2026 12:20:00 PM EDT |
| configurationType of Blank fails because most products arent available blank | changed `configurationType` to 'Decorated' | Confirmed | 04/06/2026 4:20:00 PM EDT |
| BigCommerce product metafield writes could fail intermittently with `value must be between 1 and 65535 bytes` when projected contract JSON exceeded the platform limit. | Split oversized product metafield JSON payloads into ordered `__part_N` keys during sync, clean up stale chunks when sizes change, and reassemble chunked contract values on runtime reads. | Pending | 04/07/2026 12:00:00 PM EDT |
| ProductData discovery could crash Lambda with `Maximum call stack size exceeded` or run unbounded `getProduct` fanout when vendors return very large `getProductSellable` payloads and no pagination is available. | Replace recursive payload walking with an iterative traversal, remove spread-based large-array appends, and add runtime-configurable discovery/fanout caps plus bounded getProduct concurrency to keep processing within Lambda limits. | Pending | 04/07/2026 04:45:00 PM EDT |
